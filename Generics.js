"use strict";
function mostrarDados(array) {
    array.forEach(element => {
        console.log(element);
    });
}
const A = [1, 2, 3];
mostrarDados(A);
const B = ['um', 'dois', 'três'];
mostrarDados(B);
